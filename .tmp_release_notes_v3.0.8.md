## v3.0.8

### Fixes
- Stabilized Rust MCP integration test contract to validate routing and structured payloads in both success and runtime-error cases.
- Removes environment-specific false negatives in GitHub Actions `integration-rust-mcp` lane while preserving bridge contract checks.

### Validation
- `npm run quality:check` passed.
- `npm run quality:integration:mcp` passed.
- Coverage thresholds remain green: line `81%`, branch `71.02%`, functions `76.74%`.

### Security Sanity
- Latest release commit scope checked: only `package.json` + `package-lock.json` version bump for `3.0.8`.
- Secret/token pattern scan on latest commit diff returned no matches.
