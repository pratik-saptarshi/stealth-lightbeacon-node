## v3.0.9

### Fixes
- Finalized Rust MCP integration test stability in CI by accepting structured error payloads (`error` as object/string) when runtime graph state causes non-success responses.
- Preserves deterministic contract checks for bridge routing without coupling to environment-specific graph content.

### Validation
- `npm run quality:check` passed.
- `npm run quality:integration:mcp` passed.
- Coverage gate remains green: line `81%`, branch `71.02%`, functions `76.74%`.

### Security Sanity
- Latest release commit scope: only version bump in `package.json` and `package-lock.json`.
- Secret/token pattern scan on latest commit diff returned no matches.
