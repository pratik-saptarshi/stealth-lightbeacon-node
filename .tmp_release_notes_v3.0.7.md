## v3.0.7

### CI Stabilization
- Fixed CI coverage execution portability by removing hard dependency on `/bin/zsh` in `tools/check-coverage.js`.
- Replaced shell-based CI test selection with deterministic JS file selection and explicit exclusions for environment-sensitive tests.
- Hardened MCP integration test contract in `tests/mcp.integration.test.js` to validate bridge routing/result shape without brittle runtime graph-content assumptions.

### Validation
- `npm run quality:check` passed (typecheck, CI unit lane, MCP contract lane, coverage gate).
- `npm run quality:integration:mcp` passed with Rust integration mode enabled.
- Coverage gate passed: line `81%`, branch `71.02%`, functions `76.74%`.

### Security Sanity
- Reviewed latest release commit `eec6915` scope: only `package.json` and `package-lock.json` version bump.
- Pattern scan of latest commit diff found no obvious secret/token/key material.
