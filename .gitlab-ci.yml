stages:
  - test

image: node:24

cache:
  paths:
    - .npm/

before_script:
  - corepack enable
  - corepack prepare pnpm@11.4.0 --activate
  - pnpm install --frozen-lockfile

quality-checks:
  stage: test
  script:
    - pnpm run build
    - pnpm run quality:check
